spring:
  datasource:
    platform: mysql
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/user-management-backend?createDatabaseIfNotExist=true
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect

